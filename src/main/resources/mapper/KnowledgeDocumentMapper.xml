<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.wangyangxu.ailink.mapper.KnowledgeDocumentMapper">

    <insert id="insert" parameterType="io.github.wangyangxu.ailink.model.KnowledgeDocument" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO knowledge_document
            (source_type, source_path, title, content_hash, chunk_count, embedding_model, dimension, status)
        VALUES
            (#{sourceType}, #{sourcePath}, #{title}, #{contentHash}, #{chunkCount}, #{embeddingModel}, #{dimension}, #{status})
    </insert>

    <update id="updateStats" parameterType="io.github.wangyangxu.ailink.model.KnowledgeDocument">
        UPDATE knowledge_document
        SET title = #{title},
            content_hash = #{contentHash},
            chunk_count = #{chunkCount},
            embedding_model = #{embeddingModel},
            dimension = #{dimension},
            status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="findByPath" resultType="io.github.wangyangxu.ailink.model.KnowledgeDocument">
        SELECT id, source_type, source_path, title, content_hash, chunk_count,
               embedding_model, dimension, status, created_at, updated_at
        FROM knowledge_document
        WHERE source_path = #{sourcePath}
    </select>

    <select id="findAll" resultType="io.github.wangyangxu.ailink.model.KnowledgeDocument">
        SELECT id, source_type, source_path, title, content_hash, chunk_count,
               embedding_model, dimension, status, created_at, updated_at
        FROM knowledge_document
        ORDER BY id
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM knowledge_document
    </select>
</mapper>